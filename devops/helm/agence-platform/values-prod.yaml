config:
  SPRING_PROFILES_ACTIVE: prod
  CORS_ALLOWED_ORIGINS: https://app.example.com
  VITE_API_BASE_URL: https://api.example.com/api/v1

backend:
  replicaCount: 4
frontend:
  replicaCount: 3

ingress:
  hosts:
    frontend: app.example.com
    backend: api.example.com
  tls:
    secretName: agence-prod-tls

hpa:
  minReplicas: 4
  maxReplicas: 12
