config:
  SPRING_PROFILES_ACTIVE: staging
  CORS_ALLOWED_ORIGINS: https://app-staging.example.com
  VITE_API_BASE_URL: https://api-staging.example.com/api/v1

backend:
  replicaCount: 2
frontend:
  replicaCount: 2

ingress:
  hosts:
    frontend: app-staging.example.com
    backend: api-staging.example.com
  tls:
    secretName: agence-staging-tls

hpa:
  minReplicas: 2
  maxReplicas: 8
