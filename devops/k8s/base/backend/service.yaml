apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: agence
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: agence-location
spec:
  selector:
    app.kubernetes.io/name: backend
  ports:
    - port: 8080
      targetPort: 8080
      name: http
