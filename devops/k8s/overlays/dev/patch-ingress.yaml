apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agence-ingress
spec:
  tls:
    - hosts:
        - app.localhost
        - api.localhost
      secretName: agence-dev-tls
  rules:
    - host: app.localhost
    - host: api.localhost
